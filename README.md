# modified rag-tutorial-v2 by pixegami and williamagyapong
"""
RAG Chatbot API  –  TaxAI
Run with: uvicorn main:app --reload --port 8000
http://localhost:8000

Requirements:
    1. pip install fastapi uvicorn python-multipart
    2. venv\Scripts\Activate.ps1 (activate the virtual environment)
    3. deactivate to go out from virtual environment
    (plus all deps from the original RAG project)
"""
